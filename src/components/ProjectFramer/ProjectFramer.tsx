import styles from "./ProjectFramer.module.css";
import { projects } from "@/lib/data";

const ProjectFramer = () => {
  return (
    <section>
      <h2>Project Framer YT Tutorial</h2>
    </section>
  );
};
export default ProjectFramer;
